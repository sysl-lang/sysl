// A first sysl program. Run it with:
//   sbt "syslJVM/run run examples/hello.sysl"

print("Hello, sysl!")

var width = 6
var height = 7
print("area =", width * height)

// A function: an expression body after `=`, or an indented block.
add(a: int, b: int) -> int = a + b
print("3 + 4 =", add(3, 4))

// Sum the numbers 1 through 10 with a for loop.
var sum = 0
for i in 1..10
    sum = sum + i
print("sum 1..10 =", sum)

// `if` is an expression — it yields the value of the taken branch.
print(sum, "is", if sum % 2 == 0 then "even" else "odd")

// A value struct: positional construction and field access.
struct Point
    x: int
    y: int

var p = Point(width, height)
print("point:", p.x, p.y)

// `match` picks an arm by literal, comma list, range, or a guarded wildcard.
size(n: int) -> string
    match n
        0 -> "empty"
        1, 2, 3 -> "small"
        4..99 -> "medium"
        else -> "large"

print("55 is", size(sum))
